{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Memo</h1>
    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}
