{% extends 'base.html.twig' %}

{% block content %}
    <h1>Edit Memo</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}
